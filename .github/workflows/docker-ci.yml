name: Docker Build, Scan, and Push

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-scan-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set image name to lowercase
        run: echo "IMAGE_NAME=${IMAGE_NAME,,}" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Prepare build secrets
        run: |
          mkdir -p $HOME/.secrets
          printf '%s' "${{ secrets.DATABASE_URL }}" > $HOME/.secrets/database_url
          printf '%s' "${{ secrets.NEXTAUTH_URL }}" > $HOME/.secrets/nextauth_url
          printf '%s' "${{ secrets.NEXTAUTH_SECRET }}" > $HOME/.secrets/nextauth_secret

      - name: Build Docker image
        run: |
          docker build \
            --secret id=database_url,src=$HOME/.secrets/database_url \
            --secret id=nextauth_url,src=$HOME/.secrets/nextauth_url \
            --secret id=nextauth_secret,src=$HOME/.secrets/nextauth_secret \
            -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} .
        env:
          DOCKER_BUILDKIT: 1

      - name: Clean up secrets
        if: always()
        run: rm -rf $HOME/.secrets

      - name: List Docker images
        run: docker images

      - name: Run Trivy scan
        uses: aquasecurity/trivy-action@v0.24.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          format: sarif
          output: trivy-results.sarif
          severity: HIGH,CRITICAL
          exit-code: "0"

      - name: Verify SARIF file
        run: |
          ls -lah
          test -f trivy-results.sarif

      - name: Upload Trivy results
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: trivy-results.sarif

      - name: SonarCloud Scan
        uses: sonarsource/sonarqube-scan-action@v4
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: SonarQube Quality Gate
        uses: sonarsource/sonarqube-quality-gate-action@master
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Docker image
        run: |
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}